<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sheasks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SheAsks">
        <activity android:name=".AnswersActivity" />
        <activity android:name=".Settings" />
        <activity android:name=".SearchQuestionActivity" />
        <activity android:name=".AddErrorInAppActivity" />
        <activity android:name=".AddQuestionActivity" />
        <activity android:name=".PythonCategory" />
        <activity android:name=".WebCategory" />
        <activity android:name=".GitCategory" />
        <activity android:name=".JavaCategory" />
        <activity android:name=".MainMenu" />
        <activity android:name=".LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>